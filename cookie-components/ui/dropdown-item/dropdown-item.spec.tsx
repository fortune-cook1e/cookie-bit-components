import React from 'react'
import { render } from '@testing-library/react'
import { BasicDropdownItem } from './dropdown-item.composition'

it('should render with the correct text', () => {
  // const { getByText } = render(<BasicDropdownItem />);
  // const rendered = getByText('hello from DropdownItem');
  // expect(rendered).toBeTruthy();
})
